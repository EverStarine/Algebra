# 《代数学》LaTeX 与排版规范

版本：1.1（2026-09-18）

本规范参照《分析学》的通用工程与排版体例建立，统一五卷的结构、编号、引用和版式。数学内容见 `写作规范.md`，术语与索引见 `术语规范.md`，构建命令以 `README.md` 与实际脚本为准。

## 1. 分卷与公共文件

五卷分别以 `Book1/Book1.tex` 至 `Book5/Book5.tex` 为唯一主文件，独立生成 PDF；根目录不设合订本 `Main.tex`。

- 主文件只设置卷元数据并装配书前、正文、附录和书后。
- 共同宏与版式放在 `Shared/Preamble.tex`，字体与索引样式同样在 `Shared/` 维护，不为五卷各复制一套。
- 全书共用 `Shared/References.bib`，各卷只打印自己实际引用的条目；按实际写作接入经过核对的文献。
- 正文文件拆到节为止，各节内用 `\subsection` 建立小节。纲要细目按知识主题整理为简短的小节标题，完整要点保留为注释；标题和待写提示不表示正文断言已经成立。
- 初始文件仅表示编排位置，待写提示不得被误认为数学正文或完成记录。

## 2. 路径、命名与标签

正文路径为：

```text
BookN/PartNN/PartNN.tex
BookN/PartNN/ChapterNN/ChapterNN.tex
BookN/PartNN/ChapterNN/SectionCCSS.tex
```

`BookN` 不补零，编目录与章目录的数字均两位补零；节文件的 `CC` 为章号，`SS` 为节号。节文件直接放在章目录中，不另建单文件目录。各卷独立编号，章号跨编连续。

附录路径为 `BookN/Appendices/AppendixA/AppendixA.tex`，其节文件为 `SectionA01.tex` 等。每卷附录显示 A、B、C……，不附卷号。纲要原已分节的附录保留节名，并在节内设置小节；原仅列主题的附录按这些主题补齐节标题与文件。

编、章、节、小节和除证明类以外的数学环境默认紧跟稳定标签。推荐前缀：

```text
bN:part:NN       编
bN:ch:NN         章
bN:sec:CCSS      正文节
bN:subsec:...    小节
bN:app:A         附录
bN:thm:...       定理
bN:def:...       定义
bN:lem:...       引理
bN:prop:...      命题
bN:ex:...        习题
bN:eq:...        被引用公式
bN:item:...      列表项
```

标签是稳定标识，不包含容易变化的标题文字；同一数学对象改名或移动时尽量保持标签。跨卷引用另外登记，不能因显示编号不含卷号就省略标签中的卷号。

## 3. 页面、页码与页眉

- 书名页依次显示“代数学”“卷次”“卷副标题”，共享版式统一字号与间距；长副标题可以在共享允许的范围内适当断行。
- 书名页有顶级 PDF 书签；元数据不带分析学卷名，不虚构成稿日期。
- 书前使用小写罗马页码；正文与附录共用阿拉伯页码；书后重新使用小写罗马页码。
- 书名页后保留一张计入页序但不显示页码的空白页；序言、目录及书后各单元按共享样式从奇数页开始。
- 普通正文页码在页眉：偶数页显示页码与章标题，奇数页显示节标题与页码，保留细横线。编页、章首页不设页眉与横线，页码在页脚中央。
- 序言、导读、后记、书目、索引等无编号章级单元的后续页显示自己的标题，不残留前一正文节页眉。
- 每编从奇数页开始，编题页后保留真正的空白页；“附录”有独立的无编号编题页。
- 空白页没有页眉、页脚或可见页码，但计入物理页序。
- 诗歌和短引文使用 `quote` 或 `poemquote`，出处用 `\quoteattribution{...}`。章摘要用 `chapterabstract`，不手工补空格模拟首行缩进。

编章页面样式、间距及字体由共享导言区统一，不逐章临时重设。

## 4. 标题、选读与附录

标题的文字遵守 `写作规范.md` 的教材题名约定。排版正确不等于题名自然；修改题名时核对其数学范围，保留稳定标签与原有层级。

`*` 是选读序号的一部分，不是标题文字。选读章、节、小节分别使用 `\OptionalChapter`、`\OptionalSection`、`\OptionalSubsection` 等共享入口，标题参数不放星号。数学记号本身的星号不能误判为选读标记。

附录天然选读，不再加星。每卷只有一个无编号“附录”编，用 `\BookAppendixPart` 建立顶级书签及目录入口；各附录章挂在其下，不能留在最后一个正文编中。

章节改名同步核对纲要、实际标题、当前审核记录、目录、页眉与书签；不因显示标题变化随意更换稳定标签。共享目录配色统一采用 `AlgebraBlue`、`AlgebraGray` 等样式名称，五卷不另立配色。

## 5. 定理、证明与习题

定理类中文正文采用楷体，非数学西文正体；术语首次主称仍局部恢复宋体粗体。证明、解、证明概要的标题用楷体，正文使用普通正体。数学字体不随文字环境倾斜。

定义、命题、引理、定理、断言、注记、推论、猜想、开放问题共用“章.节.本节流水号”，但引用类型彼此独立，`\cref` 必须显示实际环境名称。无节附录省略中间的零，例如 `A.1`；有节附录显示 `A.2.1`。

具名结果用可选标题，标签紧随环境开头：

```tex
\begin{theorem}[Sylow]\label{b1:thm:sylow-existence}
具体陈述。
\end{theorem}
```

不在标题手写定理编号，不在可选参数调用术语或索引宏。`proof`、`solution`、`proofsketch` 不编号，终止符由环境产生，不手写“证毕”。

习题使用独立计数器，每章从 1 开始，显示“章号.章内习题号”，不加入节号。选做题用 `optionalexercise`，与普通习题共用计数器，星号跟随题号。附录习题显示 `A.1` 等。

章末习题入口使用 `\BookExercises`，每章只有一个习题目录项与书签，不另加重复书签，也不只写缺少页眉更新的 `\section*{习题}`。骨架可以保留空入口或待写提示，不能虚构题目完成状态。

## 6. 列表与列表项引用

普通有序列表三层分别使用 `1.`、`1)`、`a)`；等价命题列表使用 `I.`、`i)`。序号均为正体，避免四层以上嵌套。数学陈述中需要引用的列表项在 `\item` 后立即设标签。

列表项引用用 `\itemref{...}`，使完整括号与序号可点击；不手写易变化的序号或仅显示半个括号的 `2)`。

## 7. 交叉引用与超链接

- 定理类引用优先 `\cref`；公式使用 `\eqref`。不另写类别名再接只有数字可点击的裸 `\ref`。
- 章、节、小节和附录分别用 `\chapref`、`\secref`、`\subsecref`、`\appref`；命令已经包含类别文字，外面不重复写“第”“章”“节”。
- 同类结果并列时各自给出完整引用，不让后续项退回裸编号。
- 不手写会因增删变化的编号；前向与后向的卷内引用均可设置链接。
- 引用宏与相邻中文、全角标点之间不加多余源码空格或 `~`。换行引起的空格在必要处以行末 `%` 抑制，不用固定 `\hspace` 补偿。
- 五卷独立 PDF 的引用须写清卷号、位置和结果名称，并登记 `跨卷引用表.md`。未实现外部链接时用准确文本引用，不能用内部引用宏假装存在跨卷目标。
- DOI、网页使用标准 URI 链接，不以 PDF JavaScript 强制阅读器的新窗口行为。

## 8. 段落、字体与数学公式

连续普通叙述段落先用空行分段，再在两段之间独立调用 `\ProseParagraphBreak`。若邻接标题、列表、公式、定理起止、图表、引文或署名等结构，不插入该命令；`proof`、`proofsketch`、`solution` 内不用此额外段距。全局 `\parskip` 保持由共享样式控制。

章首 `chapterabstract`、`learninggoals` 及其后的引导正文须用真实空行明确分段，正文首行缩进两个汉字。特别要核对列表式摘要与目标框结束后的段落状态；已有 `\parindent` 设置不保证被视作续接的段落自动缩进。不要用手工空格替代分段，也不要为某一章单独写固定水平位移。

行内公式用 `\(...\)`，行间用 `\[...\]` 或标准公式环境；`.tex` 不使用 `$...$`、`$$...$$`。这一规则不要求机械改写 Markdown 数学块。行间公式仅在需要引用时编号并设标签。

首次绑定后文复用的记号时可用 `\coloneqq`，一般恒等式、局部命名和假设仍用 `=`；不根据“定义”二字全局改写等号，源码不用 ASCII `:=`。

定理类中文字形、宋体术语粗体、非数学西文 Pagella 正体及数学 Latin Modern Math 由共享设置统一。黑板粗体与空集采用共享的 AMS MSBM 覆盖，不另设不同的行内和行间数学字体；`\mathrm`、`\mathit`、`\mathbf` 等取相应数学字形，公式中的 `\text` 只用于说明文字。

空集源码统一为 `\varnothing`。需要粗正体数学字母或数字时使用 `\mathbf` 或 `\symbfup`，粗斜体用 `\symbfit`；不能仅凭 `\boldsymbol` 命令名认定加粗已经生效。

字体、尺寸、颜色和全书段距只修改共享设置，不在各章零散覆盖。临时无法取得某字体时先检查现有配置与安装，不为一个章节更换全书字形。

固定运算符用预定义命令或 `\operatorname`，变量按其数学角色排印。复形微分的符号与升降次由 `术语规范.md` 统一，不继承分析学项目对距离函数等专用符号的个别约定。

数学括号成对使用。只要定界符形成真实的包含层次，外层就须比内层大一级，即使 `\left`、`\right` 的自然适配会把两层排成同样大小，也要显式使用 `\bigl`／`\bigr`、`\Bigl`／`\Bigr` 等调整。例如 `f\bigl(g(x)\bigr)`，更深一层写成 `f\Bigl(g\bigl(h(x)\bigr)\Bigr)`；方括号包元组、集合括号包元组也按此处理。矩阵环境自带的括号同样计入层次，应另作视觉检查。正规闭包的双尖括号、范数的双竖线是一个复合符号，不拆成两层括号；TeX 命令参数的花括号及数学模式的 `\(`、`\)` 不属于印刷定界符。

括号层级以最终 PDF 中可辨认的字形高度为准，不能仅凭源码使用了不同大小命令便认定合格。若普通圆括号与 `\bigl`／`\bigr` 的视觉差别不明显，应把外层增至 `\Bigl`／`\Bigr`，并同步增大更外层；例如本卷定义1.1.3后的三层复合写为 `f\biggl(g\Bigl(h(x)\Bigr)\biggr)`。修改后须放大检查对应公式图片。

过高的行内分式应考虑转为行间公式，不为统一外观破坏行距。绝对值或范数因子相乘，以及相邻因子容易混淆的乘积，显式用 `\cdot`；算子作用、复合、标量作用和普通无歧义单项式不机械插点。

行内映射用 `\rightarrow`、`\mapsto`，行间用 `\longrightarrow`、`\longmapsto`。交换图用 `tikzcd`，并核对箭头方向、作用次序、交换或反交换条件。集合构造式留适当数学间距，例如 `\{\,x\in X\mid P(x)\,\}`。

## 9. 图、表与算法

作为可引用对象的图、表、算法均设置编号、标题和标签。对应类别首次实际出现后再加入目录，不生成无内容的图表目录。编内阅读依赖图与证明中的局部交换图不必机械变为浮动插图。

目录只收短题名：使用 `\caption[短题名]{短题名。补充说明}`。表格列宽依据内容分配，不只确认没有报错，还核对版心利用与可读性。

算法写出输入、输出、步骤及适用条件。插入图表、算法或交换图后检查位置、缩放、跨页、文字辨识和阅读顺序；空章不预制装饰对象。

## 10. 书目、索引与书后

共同书目采用 `biblatex + biber`。正文 `\cite{key}`，书后通过 `\PrintBookBibliography` 自动调用 `\printbibliography` 或保留空书目入口；正式卷 PDF 不用 `\nocite{*}` 灌入全库。初始数据库无条目、正文无引文时，构建脚本可跳过 Biber；出现引文或书目变动后必须按脚本重新生成，不能复用陈旧 `.bbl`。

三类索引为 `chinese`、`foreign`、`symbols`，分别生成；中文按拼音、外文按原文排序，符号按首次登记顺序。每个索引单独运行处理器，样式为 `Shared/algebra.ist`，不把多个 `.idx` 合并调用。

索引正文统一用 `\small` 双栏排印，栏底自然收束；页码右对齐，多页码用半角逗号加空格分隔。空索引不沿用历史 `.ind`；非空索引须实际生成并读入。检查索引日志的拒收与警告，不能只凭处理器返回成功判定收词完整。

后记、书目与索引在书签中保持书后顶级单元，不挂在最后一个附录下。没有实际书目或索引内容时，空入口只表示待写骨架，不宣称已经有完整书后资料。

## 11. 构建与验收

根目录 `build.ps1` 负责选择和构建五卷，各卷 `build-BookN.ps1` 提供独立入口。构建使用 XeLaTeX、按需要运行 Biber、分别处理三类索引，并再次运行 XeLaTeX 至引用稳定；完整命令见 `README.md`。

输出隔离于 `tmp/build/BookN/`。通过检查后，PDF 复制到根目录作为成品，同时在卷目录提供 PDF 与 SyncTeX 供编辑预览；卷目录预览与辅助产物由 Git 忽略，不把 `.aux`、`.bbl`、`.idx` 等散入源码。

阶段验收至少包括：输入链完整、编译无错误、无未定义或重复标签、引用和书签可解析、索引无拒收、无明显溢出版心、页码分区与附录入口正确。共享样式变化影响五卷，应全部重编；仅改某卷正文时构建受影响卷即可。结构骨架的编译验收不等于正文数学审核。
