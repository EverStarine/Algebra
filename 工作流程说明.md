# 《代数学》项目工作流程

版本：1.0（2026-09-17）

本文件参照《分析学》项目的通用工作流程建立，只保留可复用的方法，不继承原项目的章节进度、模型绑定、联网故障记录、自动上传安排或旧仓库状态。

## 1. 任务定界与主管文件

开始前查看工作区状态，确认本轮目标卷、编、章和节，阅读相应纲要及规范。优先阅读和编辑 `.tex` 源码；PDF 用于检查编译与版面，不能代替源文件中的证明、标签和引用核对。

| 内容 | 主管文件 |
|---|---|
| 总体结构、先修与证明任务 | `计划纲要.md` |
| 内容组织、证明职责与习题 | `写作规范.md` |
| 路径、编号、样式与引用接口 | `格式规范.md` |
| 术语、原文、异名和数学约定 | `术语规范.md` |
| 书目字段与版本 | `书目维护注意事项.md` |
| 操作流程与可复现故障 | 本文件 |
| 各章状态、数学疑点与审核意见 | `审核进度.md` |
| 实际跨卷引用 | `跨卷引用表.md` |
| 作者与材料使用边界 | `版权声明.md` |
| 实际构建命令与工程入口 | `README.md`、构建脚本 |

同一规则在主管文件完整表述，其他文件引用，不维护相互漂移的多份版本。没有当前任务需要时，不把联网同步、全书审稿或临时文件清理固定插入每一轮编辑。

## 2. 编辑层级与单一真源

输入顺序为：

```text
BookN.tex
  → FrontMatter / PartNN / Appendices / BackMatter
  → ChapterNN
  → SectionCCSS.tex
```

主文件装配全卷，编文件装配各章，章文件放标题、导语、节输入和章末入口，节文件是正文最小文件层级。附录按对应的章、节入口装配。共用样式只改 `Shared/`。

纲要的卷、编、章、节标题决定目录和文件，节下项目符号按知识主题整理成简短小节标题；完整原要点仍保留为写作注释。原仅列主题而未分节的附录据此补齐节标题和节文件。标题只表示写作安排，不表示已证陈述。保留原纲要，不把纲要后附的先修表、版本记录等误认成新增正文卷。

调整结构前检查实际证明依赖。改动后同步输入链、相关纲要位置、当前审核记录和跨卷引用；保留仍指向同一对象的稳定标签。章名须与摘要、一级节、小结共同核对，不能只搜索标题字面。

跨文件迁移后检查全部 `\input`、编辑器根文件指令、文档中的路径及引用目标，不能保留失效的单文件节目录。

## 3. 初次撰写

1. 读取目标节纲要、相邻已写正文和真实先修，确定要完成的构造、定理、算例与边界。
2. 核定新术语，沿用纲要明确主称，区分定义、异名和不同语境。
3. 按“问题—对象—构造—证明—算例”展开，落实本节承担的证明任务；不把要点略作扩句就标为初稿完成。
4. 为正式标题、数学环境、必要公式与引用列表项设置稳定标签，首次术语附完整原文。
5. 完成章末小结、代表习题及解答或明确提示，检查习题是否原样复述正文。
6. 检查关键假设、左右作用、有限性、特征、复形符号以及算例计算，记录仍需数学复核的疑点。
7. 更新章节状态和实际跨卷引用，构建受影响卷，核对必要输出。

骨架、初稿、正式审核、定稿分别记录。首次撰写的必要自检不自动计作正式审核，不借“尚未审稿”省略应有的完整证明。

## 4. 处理审稿与数学疑点

逐项记录意见定位、类型、修改、核验办法与结果。核对原始定义和实际假设，避免为了修复局部例子改动全书约定。

涉及后文调用时追踪标签和证明输入，更新相关处而非只修被指出的一行。定理主要证明位置改变时，检查前后是否出现循环依赖，尤其是第三卷局部结构与第四卷同调模块之间的接口。

模型或工具的建议不等于数学事实。外部检索、符号计算或有限算例只为相应范围提供证据；不能把截断计算当作一般定理的证明。需要人工核定或尚未解决的问题如实记入审核记录，不标为已闭环。

## 5. 编译顺序

优先使用项目构建脚本，根脚本默认五卷，也支持按卷选择；具体参数见 `README.md`。分卷独立输出到 `tmp/build/BookN/`。

构建流程为 XeLaTeX 首轮，存在实际引文时运行 Biber，分别处理三类索引，再运行 XeLaTeX 直至引用与目录稳定。空书目和空索引按脚本分支处理，不能让历史输出混入新 PDF。

每轮启用 SyncTeX。验证成功后复制根目录成品和卷目录编辑预览，不将辅助文件复制回正文路径。最终日志仍有未定义引用、重复标签或重跑提示时不能把结果记为稳定。

优先检查和修复现有 TeX Live、命令路径与配置，不为普通构建错误另装一套工具链。编译失败首先记录首个有效错误，随后处理其后果，避免围绕连锁报错反复改文件。

## 6. 检查范围与止点

内容修改先检查源码和构建日志；版式修改再按影响范围查看 PDF。代表页面包括目录、卷首、普通页、章首页、附录入口、书后以及实际改动涉及的长公式、图表或索引页。

共享样式、字体或构建逻辑改变时重编五卷；局部正文只需重编受影响卷。只改规范且未改变源码规则时，不机械重编全书。

图像检查用于解决实际版面问题，不为每轮文字小改渲染全书。完成当前范围的必要验证后交付，不无限扩大措辞优化或正式审稿，也不把编译通过等同于数学正确。

章节或整卷阶段收尾时检查标题与范围、依赖图、选读边界、术语原文、索引、书目、图表需求和文风一致性；只纠正实际断层，不把不同数学任务加工成同样段长。

## 7. 附录检查

- 每卷附录从 A 独立编号，不带卷号，不加选读星号。
- “附录”是独立顶级编入口，各附录章挂在其下。
- 明确基本对象、代表结果、可核验例子、先修与后续边界。
- 不把附录中未证明的结论反向作为主干隐含前提。
- 外部分析、几何等知识确有需要时列清最小输入，不因另一项目存在相关标题便宣称已经覆盖。

## 8. 文献采集与接入

书目数据库初始为空。开始引用时，先核实文献真实存在、实际版本和定位信息，再写入 `Shared/References.bib`；不同版本的数据不能混合。

正式资料优先由出版社、期刊、DOI、作者或机构公开来源核验；网络摘要和搜索结果不证明全文已经读过。取得全文后记录文件来源、版本、实际阅读范围及使用位置。

内部参考资料置于 `参考文献原文件/`，不得因提交源码而自动上传第三方全文。需要公开的来源记录应保留链接、元数据与必要说明，不包含访问凭据或受限材料。

纲要中的历史资料编号、未附带术语表与审校记录先保持“未取得／待核”。只有实际查阅过的范围可写“已核验”，不能将纲要既有措辞复制为本轮的实读结论。

数据库变更按 `书目维护注意事项.md` 核对数据模型与实际排版。只新增正文引文时运行目标卷书目和构建即可，不无谓重做全部条目的检索。

## 9. 跨卷引用

实际跨卷调用同步登记引用处、目标处、稳定标签、内容概要、当前编号及证明状态。骨架阶段没有实际正文调用，不预填“已核对”记录。

合法的跨卷先修按纲要分流，不受出版先后简单限制。区分“已证调用”“前瞻陈述”“计划接口”，避免把第四卷尚待撰写的证明写成第三卷已有工具；不要为避免跨卷而重复或改变主要证明。

## 10. 临时文件

`tmp/` 存放构建、检验、预览与临时研究产物，由 Git 忽略。需要长期保存的源码、规范与数据放入明确位置；用于说明结论的核验记录在任务记录中保留足够复现信息。

清理只针对已核实路径的可重建产物，不以递归删除整个工作区代替清理，不误删参考材料或未提交正文。

## 11. Git 与 GitHub

本项目目标仓库为 `EverStarine/Algebra`。初始化、提交和上传按当前明确任务执行，不继承《分析学》项目的每日推送或逐章上传授权。

提交前查看状态与差异，确认只包含本次授权范围的源码、规范、必要数据和通过验证的成品；不上传临时辅助文件、凭据或第三方资料原文。共享文件可能包含其他任务的修改，不将它们悄悄覆盖。

普通上传前核对仓库地址与分支，不对错误目标推送。用户已授权的同一上传不反复询问；若发生实际权限或自动审核阻断，保留本地工作并明确报告阻断原因，不绕过审核或关闭安全设置。

推送后核对真实远端提交与本地提交一致。本地提交成功不等于上传成功。遇到非快进冲突先核对远端变化，不用强制推送清除他人提交，也不在未核实路径和差异时批量回退。

## 12. 问题记录模板

后续可复现流程问题在此追加，章节数学问题放 `审核进度.md`。

- 日期、状态、类型：
- 影响卷及文件：
- 首个有效错误或现象：
- 已核实根因：
- 处理与修改：
- 验证范围、方式与结果：
- 尚存问题与后续注意事项：

当前仅建立项目初始规则，未迁入分析学的历史故障记录。
