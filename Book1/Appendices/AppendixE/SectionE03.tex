% !TeX root = ../../Book1.tex
% 纲要标题：### E.3 多项式的实根与实代数
% 纲要位置：计划纲要.md，第 930 行。
% BEGIN APPENDIX OUTLINE SYNC
% #### E.3.1 中间值性质与 Sturm 根计数
%
% #### E.3.2 传递原理、量词消去与半代数集合
% END APPENDIX OUTLINE SYNC

\section{多项式的实根与实代数}
\label{b1:sec:E03}

实根计数可以只用多项式除法与符号比较完成。本节先从实闭域上的因式分解推出中间值性质，再给出 Sturm 序列的计数公式。即使域中有大于所有整数的元素，证明也仍然适用。

% 纲要内容（仅作写作计划，尚非教材正文）：
%
% * 多项式的中间值性质与 Sturm 根计数作为可手算实例
% * 实闭域上的传递原理和量词消去只作预告；半代数集合与正性证书转第三卷附录 G
% * 不在本附录展开完整模型论、赋值理论或二次型分类；完整证明按专题教材安排
%
% ---
%

\subsection{中间值性质与 Sturm 根计数}
\label{b1:subsec:E03:01}

\begin{theorem}[多项式中间值性质]\label{b1:thm:real-closed-ivt}
设 \(R\) 实闭，\(a<b\) 属于 \(R\)，且 \(f\in R[X]\) 满足 \(f(a)f(b)<0\)。则存在 \(c\in R\)，使 \(a<c<b\) 且 \(f(c)=0\)。
\end{theorem}
\begin{proof}
由\cref{b1:cor:real-polynomial-factorization}，将 \(f\) 分成常数、一次因子和处处为正的二次因子。若没有实根位于 \((a,b)\)，每个一次因子在两端的符号相同，二次因子在两端均为正，故乘积 \(f(a)f(b)>0\)，矛盾。
\end{proof}

这个证明也说明：非零多项式的符号在不含根的区间上固定。无需先假设一般连续函数具有中间值性质。

\ProseParagraphBreak
先设 \(f\in R[X]\) 为无重根的非常数多项式。它与 \(f'\) 互素。令 \(f_0=f\)、\(f_1=f'\)，再用带负号的余数递推
\begin{equation}\label{b1:eq:sturm-remainders}
f_{j-1}=q_jf_j-f_{j+1},\qquad \deg f_{j+1}<\deg f_j,
\end{equation}
直到最后一个非零余数 \(f_m\)。次数严格下降保证终止，互素性保证 \(f_m\) 为非零常数。所得序列称为 \(f\) 的\term[Sturm-xulie]{Sturm 序列}[Sturm sequence]。对 \(x\in R\)，把 \(f_0(x),\ldots,f_m(x)\) 中的零项删去，记剩余符号列的变号次数为 \(V(x)\)。
\symidx{\(V(x)\)：Sturm 序列在一点的变号次数}

\begin{theorem}[Sturm]\label{b1:thm:sturm-count}
在上述条件下，若 \(a<b\) 且 \(f(a)f(b)\neq0\)，则 \(f\) 在 \((a,b)\) 中的根数为 \(V(a)-V(b)\)，每个根只计一次。
\end{theorem}
\begin{proof}
相邻两个 \(f_j\) 不可能有公共根：若同时为零，\eqref{b1:eq:sturm-remainders}会把零向前或向后传递，最终迫使非零常数 \(f_m\) 为零。若 \(f_j(c)=0\)、\(1\leq j<m\)，则递推式给出 \(f_{j-1}(c)=-f_{j+1}(c)\neq0\)。在 \(c\) 两侧足够近而不跨越其他根的位置，这两个邻项符号不变且相反。因此不论中间项取何符号，三项总贡献一次变号；在 \(c\) 处删去零项也贡献一次。多个不相邻的中间项同时为零时，这些局部变化涉及的相邻符号对互不重叠，结论仍成立。

若 \(f(c)=0\)，无重根性给出 \(f'(c)\neq0\)。写成 \(f(X)=(X-c)g(X)\)，有 \(g(c)=f'(c)\)。在 \(c\) 左侧足够近处，\(f\) 与 \(f'\) 异号；右侧足够近处同号。因此越过 \(c\) 时，最前一对符号的变号次数恰减少一，其余部分按前段不改变总变号数。

所有 \(f_j\) 的根总共只有有限个，可按序排列。在相邻根之间，各 \(f_j\) 的符号由\cref{b1:cor:real-polynomial-factorization}保持不变。选相邻端点的中点即可逐段比较，不需任何完备性假设。从 \(a\) 到 \(b\) 的变号数只在 \(f\) 的根处各减少一，端点若有中间项为零，前段的删零规则仍有效。于是总减少量就是所求根数。
\end{proof}

若原多项式有重根，先以 \(h=f/\gcd(f,f')\) 代替 \(f\)。实闭域特征为零；对每个不可约因子比较导数中的重数，可知 \(h\) 恰保留各因子一次，因此与 \(f\) 有相同的互异根，且无重根。上述算法于是计算互异实根数；若要计算重数，另保留原分解中的指数。

\ProseParagraphBreak
例如 \(f=X^3-X-1\) 的 Sturm 序列可以取
\[
f_0=X^3-X-1,\quad f_1=3X^2-1,\quad
f_2=\tfrac23X+1,\quad f_3=-\tfrac{23}{4}.
\]
在 \(1\) 处符号为 \((-,+,+,-)\)，变号数为二；在 \(2\) 处为 \((+,+,+,-)\)，变号数为一，所以 \((1,2)\) 内恰有一个实根。对任意实闭域，这份除法及符号证书给出相同结论。若工作域中的系数能精确运算并能判定符号，Sturm 方法便是可执行算法；抽象域的定义本身不保证存在可用的机器表示。

\subsection{传递原理、量词消去与半代数集合}
\label{b1:subsec:E03:02}

一个\term[bandaishu-jihe]{半代数集合}[semialgebraic set]是实闭域 \(R\) 的 \(R^n\) 中，由有限多个多项式等式与严格不等式通过有限次并、交、补得到的集合。系数所在的域须随问题指定；例如圆盘可写为 \(x^2+y^2\leq1\)，其中非严格不等式可拆成严格不等式与等式。

\ProseParagraphBreak
消去一个存在量词相当于做坐标投影。例如
\[
\exists y\ (y^2=x)\quad\Longleftrightarrow\quad x\geq0
\]
在实闭域中成立：平方非负给出必要性，正元素有平方根给出充分性。对一般有限多项式条件也能消去量词，是实闭域的\term[liangci-xiaoqu]{量词消去}[quantifier elimination]定理；这里仅预告其内容，不证明一般消去算法，也不把它当作上述 Sturm 证明的前提。

\ProseParagraphBreak
相应的\term[chuan-di-yuanli]{传递原理}[transfer principle]必须保留语言和参数的限制：用 \(0,1,+,-,\cdot,<\) 表述、没有自由变量也没有额外参数的一阶句子，在所有实闭域中真值相同；若实闭域 \(R\subseteq S\)，则带 \(R\) 中参数的一阶公式，在取自 \(R\) 的自由变量值上也有相同真值。它不表示任意关于实数的分析陈述都能转移。例如“每个非空有上界的子集都有上确界”量化的是任意子集，不是这里的一阶域语言，而\secref{b1:sec:E02}已给出该性质失效的实闭域。

\ProseParagraphBreak
半代数集合的投影、量词消去和正性证书将转第三卷附录 G；完整理论可从\cite{BochnakCosteRoy1998RealAG}继续阅读。这里不展开模型论、赋值理论或二次型分类。读者若只需要核验一元多项式的实根数，前一小节的除法表、端点符号和\cref{b1:thm:sturm-count}已经组成独立可核验的计算过程。
