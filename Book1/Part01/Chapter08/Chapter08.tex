% !TeX root = ../../Book1.tex
% 纲要标题：## 第8章　自由群与生成关系
% 纲要位置：计划纲要.md，第 328 行。

\chapter{自由群与生成关系}
\label{b1:ch:08}
\BookChapterNumber{b1:ch:08}

\begin{chapterabstract}
本章从字及其消去构造自由群，用泛性质说明如何任意指定生成元的像，再用正规闭包把关系加入构造。自由积保留不同群的内部乘法而不强迫它们交换。最后的选读节把子群元素解释为陪集图中的闭路径，证明自由群的子群仍自由，并算出有限指数子群的秩。
\end{chapterabstract}

\begin{learninggoals}
\begin{itemize}
\item 能用约化字判定自由群中的等式，并从商集合的构造说明结合律。
\item 能由呈示定义同态，区别“关系成立”与“呈示已经证明”。
\item 理解自由积和直积的不同约束；选读时能由陪集图及生成树构造子群自由基。
\end{itemize}
\end{learninggoals}

本章主要使用群同态、商群及生成子群，三个非选读节不依赖前一章的合成列理论。自由群将“先生成、后加关系”的方法表达成一个具体构造；\chapref{b1:ch:09}将在阿贝尔群中把同一想法化成整数关系矩阵。图论选读节可以在掌握约化字后独立阅读，所需图和树的事实均随证明说明。

\input{Part01/Chapter08/Section0801}
\input{Part01/Chapter08/Section0802}
\input{Part01/Chapter08/Section0803}
\input{Part01/Chapter08/Section0804}

\begin{chaptersummary}
自由群的约化字兼顾了构造和计算：它既使群公理成立，也给出唯一标准形。呈示通过关系字的正规闭包描述商群，验证呈示时还须排除多余元素。自由积允许两个因子独立映入目标群，直积则额外要求两因子的像交换。Nielsen--Schreier 证明把子群自由基具体落实为生成树外的边；有限指数时，数边便得到秩公式。
\end{chaptersummary}
\BookExercises

\begin{exercise}\label{b1:ex:08-torsion-free}
证明自由群的非单位元都具有无限阶。提示：先把一个非空约化字写成 \(uvu^{-1}\)，其中 \(v\) 非空，且其首尾字母不互逆。
\end{exercise}
\begin{solution}
若约化字首尾互逆，就把这两个字母分别移入 \(u\) 及 \(u^{-1}\)，对中间字重复。长度每次减少二；非空约化字不能最终只剩一对相邻逆字母，所以最后得到非空 \(v\)，长度为一或首尾不互逆。此时 \(v^m\) 对每个正整数 \(m\) 仍约化且非空。原字是 \(uvu^{-1}\) 的约化串接，故 \(uv^mu^{-1}\) 也在相同的两个接缝处不消去。由\cref{b1:lem:unique-reduced-word}，非空约化字不能表示单位，因此 \(uv^mu^{-1}\ne1\)。而 \((uvu^{-1})^m=uv^mu^{-1}\)，所以原字的任何正幂都非单位。
\end{solution}

\begin{exercise}\label{b1:ex:08-squares-embedding}
在 \(F\bigl(\{a,b\}\bigr)\) 中，证明 \(a^2,b^2\) 自由生成一个真子群。
\end{exercise}
\begin{solution}
由\cref{b1:thm:free-group-universal}，存在唯一同态 \(F\bigl(\{x,y\}\bigr)\to F\bigl(\{a,b\}\bigr)\) 发送 \(x\mapsto a^2,y\mapsto b^2\)。一个非空约化字可按相同生成元的最大块写成 \(x^{m_1}y^{n_1}\cdots\) 一类交替表达式，所有出现的指数非零；端部可以从任一类型开始或结束。替换后各指数乘二，交替的非零块之间没有消去。由\cref{b1:lem:unique-reduced-word}，像非单位，故同态的核只有单位元；它因而单射，并给出到 \(\langle a^2,b^2\rangle\) 的同构。映射到 \(C_2\) 时令 \(a\mapsto1,b\mapsto0\)，则 \(a^2,b^2\) 的像均为零，而 \(a\) 的像非零。因此生成子群不含 \(a\)，是真子群。
\end{solution}

\begin{exercise}\label{b1:ex:08-quaternion-presentation}
证明
\[
Q_8\cong\langle x,y\mid x^4=1,\ y^2=x^2,\ yxy^{-1}=x^{-1}\rangle.
\]
\end{exercise}
\begin{solution}
按\eqref{b1:eq:quaternion-relations}及其相邻的反向乘法规则，\(i^2=j^2=-1\)、\(ji=-ij\)。因此 \(i^4=1\)、\(j^2=i^2\)，且 \(jij^{-1}=-i=i^{-1}\)。把 \(x,y\) 分别映为 \(i,j\)，由\cref{b1:prop:presentation-homomorphism}得到呈示群到 \(Q_8\) 的同态；其像含 \(i,j\)，从而含 \(\pm1,\pm i,\pm j,\pm ij\)，故满射。在呈示群中，先由 \(x^4=1\)、\(y^4=x^4=1\) 把负指数改成非负指数；再用 \(yx=x^{-1}y\) 把 \(y\) 推到右端，\(y^2=x^2\) 把 \(y\) 的指数减至零或一，再由 \(x^4=1\) 把 \(x\) 的指数减至 \(0,1,2,3\)。所以每个元素是 \(x^iy^\varepsilon\)，至多八个。已有满同态到八阶群，因而同构。关系 \(y^2=x^2\) 不能改成 \(y^2=1\)；后者正是\cref{b1:prop:dihedral-presentation}中 \(n=4\) 的呈示，给出 \(D_8\)。
\end{solution}

\begin{exercise}\label{b1:ex:08-two-reflection-presentation}
对 \(n\ge3\)，证明
\[
\bigl\langle a,b\mid a^2=b^2=(ab)^n=1\bigr\rangle\cong D_{2n}.
\]
\end{exercise}
\begin{solution}
在左边令 \(r=ab,s=a\)。有 \(r^n=s^2=1\)，并且
\(srs^{-1}=ba=r^{-1}\)，由\cref{b1:prop:dihedral-presentation}，\(D_{2n}\) 正由这三个关系呈示，再用\cref{b1:prop:presentation-homomorphism}，得到 \(D_{2n}\) 到左边呈示群的同态。反向在 \(D_{2n}\) 中令 \(a=s,b=sr\)，则 \(a^2=s^2=1\)、\(b^2=srsr=r^{-1}r=1\)、\(ab=r\)，所以也满足 \((ab)^n=1\)；同一呈示的映射性质给出反向同态。两方向在相应生成元上复合为恒等，故互为逆同构。这个呈示强调两个反射，正文的呈示强调旋转与反射。
\end{solution}

\begin{exercise}\label{b1:ex:08-free-product-abelianization}
对群 \(G\) 记 \(G_{\mathrm{ab}}=G/G'\)。证明
\[
(G*H)_{\mathrm{ab}}\cong G_{\mathrm{ab}}\times H_{\mathrm{ab}}.
\]
\end{exercise}
\begin{solution}
记 \(F=G*H\)、\(B=F/F'\)。将 \(g\in G,h\in H\) 分别送到 \((gG',1)\)、\((1,hH')\)，由\cref{b1:thm:free-product-universal}得到满同态 \(u:F\to G_{\mathrm{ab}}\times H_{\mathrm{ab}}\)。目标阿贝尔，故
\[
u\Bigl([x,y]\Bigr)=\Bigl[u(x),u(y)\Bigr]=1,
\]
从而 \(F'\subseteq\ker u\)。由\cref{b1:thm:quotient-universal}，它唯一地诱导 \(\bar u:B\to G_{\mathrm{ab}}\times H_{\mathrm{ab}}\)。

反向把因子 \(G,H\) 映入 \(F\) 后再商到 \(B\)。由\cref{b1:prop:derived-quotient}，\(B\) 阿贝尔；上述交换子计算说明这两个映射分别消去 \(G',H'\)，所以同一商群泛性质给出 \(\alpha:G/G'\to B\)、\(\beta:H/H'\to B\)。定义
\[
v(gG',hH')=\alpha(gG')\beta(hH').
\]
两像在 \(B\) 中交换，所以 \(v\) 保持乘法。复合 \(\bar uv\) 固定直积的两个因子，\(v\bar u\) 固定 \(B\) 中 \(G,H\) 的像；这些像分别生成相应群，因此两复合都是恒等。
\end{solution}

\begin{exercise}\label{b1:ex:08-redundant-generator}
设 \(P=\langle S\mid R\rangle\)，\(w\in F(S)\)，并取新符号 \(t\notin S\)。证明增加生成元和关系所得
\[
Q=\bigl\langle S\sqcup\{t\}\mid R,\ t=w\bigr\rangle
\]
与 \(P\) 同构。说明为何只增加 \(t\) 而不增加关系通常会改变群。
\end{exercise}
\begin{solution}
映射 \(S\to P\) 取原有生成元在 \(P\) 中的像，并令 \(t\mapsto\overline w\)，满足 \(Q\) 的全部关系，故\cref{b1:prop:presentation-homomorphism}给出同态 \(Q\to P\)。由 \(S\) 在 \(Q\) 中仍满足 \(R\)，同一命题也给出 \(P\to Q\)。复合在 \(S\) 上均为恒等，在 \(Q\) 的 \(t\) 上则由 \(t=w\) 得到恒等，因此两者互逆。若只加新符号，在自由积的呈示公式\eqref{b1:eq:free-product-presentation}中取第二个群为 \(\mathbb Z=\langle t\mid\varnothing\rangle\)：两组生成符号不交，合并后的关系仍只有 \(R\)，所以所得群为 \(P*\mathbb Z\)。例如 \(P=1\) 时它变为 \(\mathbb Z\)，所以缺少关系会引入新的自由选择。
\end{solution}

\begin{optionalexercise}\label{b1:ex:08-index-three}
设 \(F=F\bigl(\{a,b\}\bigr)\)，令 \(H\) 为同态 \(F\to C_3\)、\(a\mapsto1,b\mapsto0\) 的核。用 Schreier 图写出 \(H\) 的自由基。
\end{optionalexercise}
\begin{solution}
陪集为 \(H,Ha,Ha^2\)。三条正向 \(a\) 边依次构成三角形，每个顶点另有一个 \(b\) 环。取前两条 \(a\) 边为树，树代表字为 \(1,a,a^2\)。非树 \(a\) 边给出 \(a^3\)，三个 \(b\) 环分别给出
\[
b,\qquad aba^{-1},\qquad a^2ba^{-2}.
\]
具体地，按\eqref{b1:eq:schreier-basis}，末条 \(a\) 边给出 \(a^2a\cdot1^{-1}=a^3\)，各顶点的 \(b\) 环给出 \(a^kba^{-k}\)（\(k=0,1,2\)）。\cref{b1:lem:graph-loop-free}断言每条非树边恰对应一个自由基元素，而\cref{b1:thm:nielsen-schreier}证明中的标签映射是闭路径群到 \(H\) 的同构，所以这四个元素确实构成自由基。由\cref{b1:thm:first-isomorphism}，该满同态的核指数为三；\cref{b1:thm:schreier-rank}也给出秩 \(1+3(2-1)=4\)。
\end{solution}

\begin{optionalexercise}\label{b1:ex:08-infinite-rank-kernel}
设 \(F=F\bigl(\{a,b\}\bigr)\)，同态 \(F\to\mathbb Z\) 发送 \(a\mapsto1,b\mapsto0\)。证明其核自由，且自由基为 \(\{a^kba^{-k}\mid k\in\mathbb Z\}\)。
\end{optionalexercise}
\begin{solution}
核的右陪集由整数 \(k\) 标记，代表字取 \(a^k\)。正向 \(a\) 边从 \(k\) 指向 \(k+1\)，正向 \(b\) 边是各顶点的环。所有 \(a\) 边组成一条双向无限的树，所有非树边正好是 \(b\) 环。由\eqref{b1:eq:schreier-basis}，位于 \(k\) 的 \(b\) 环对应 \(a^kba^{-k}\)。\cref{b1:lem:graph-loop-free}与\cref{b1:thm:nielsen-schreier}证明中的标签同构说明，所有这些非树边对应的元素构成 \(H\) 的自由基。若 \(H\) 有有限生成集，这些生成元的约化字总共只涉及有限个基字母，其乘积不可能产生其余基字母，矛盾。因此该核不是有限生成群，尽管母群只需两个生成元。
\end{solution}
