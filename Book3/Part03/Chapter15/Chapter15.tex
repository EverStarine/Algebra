% !TeX root = ../../Book3.tex
% 纲要标题：## 第15章　正则序列与 Koszul 复形
% 纲要位置：计划纲要.md，第 1962 行。

\chapter{正则序列与 Koszul 复形}
\label{b3:ch:15}
\BookChapterNumber{b3:ch:15}

\begin{chapterabstract}
一个方程的乘法核可以用二项复形表示；几个方程的逐次正则性则由 Koszul 复形统一记录。本章先辨析模上非零因子、逐次商与末端非零的要求，证明局部有限模的正则序列可以重排，再从外代数写出 Koszul 微分、张量积符号及逐元加入的同调正合列。在此基础上证明正则序列的正合性，并在 Noether 局部有限模情形证明逆命题。最后通过具体方程和 Tor 计算，比较独立方程、冗余关系与商环中的重数。
\end{chapterabstract}

\begin{learninggoals}
\begin{itemize}
\item 在每次取商后检验正则性，并说明局部性、有限性和末端非零条件的作用。
\item 写出低阶 Koszul 微分，核对张量积、外积和移位中的符号。
\item 用逐元加入的正合列证明正则性与 Koszul 同调消失之间的关系。
\item 区分自由消解与一般模上的正合 Koszul 复形，完成若干 Tor 的直接计算。
\end{itemize}
\end{learninggoals}

本章使用\chapref{b3:ch:04}的关联素理想、\cref{b3:thm:krull-intersection}和第二卷第9章的外代数。复形、自由与投射消解、Ext、Tor 及维数平移的系统讨论安排在第四卷第5—8章；本章与下一章直接建立当前论证需要的复形、同伦、Tor 和 Ext 构造及基本正合性质，可以据此继续阅读。本编不要求谱序列或导出范畴。

\ProseParagraphBreak
正则序列及 Koszul 复形可对照松村英之的《Commutative Ring Theory》\cite[\S16, pp. 123--129]{Matsumura1989CommutativeRingTheory}，其中 Theorems 16.4--16.5 分别给出逐元加入的同调正合列与正合性判据。本章把这些映射和符号逐项写出；一元情形应当始终作为核对一般公式的基准。

% 纲要内容（仅作写作计划，尚非教材正文）：
%
% > 本编使用第四卷第5–8章的复形、投射消解、Ext 与 Tor，并明确包括新增第8.5节的最低限度维数平移；所需模论与范畴语言见预备知识总表。不要求先学谱序列、导出范畴、t-结构，也不要求先读第四卷第10章的全部同调维数理论。
%

\input{Part03/Chapter15/Section1501}
\input{Part03/Chapter15/Section1502}
\input{Part03/Chapter15/Section1503}
\input{Part03/Chapter15/Section1504}

\begin{chaptersummary}
正则序列的条件施加在逐次商上，不能只在原环中检验各元素。局部有限模中，Nakayama 引理保证商不消失，Krull 交定理则使相邻元素可以交换次序。

\ProseParagraphBreak
Koszul 微分由删去外积因子和交替符号构成；微分平方为零来自同一对因子的两种删法相消。加进一个元素，相当于把原复形与其移位组合起来，其连接映射就是该元素的乘法。这条正合列既证明正则序列给出消解，也把局部逆命题归结为有限同调模上的 Nakayama 引理。

\ProseParagraphBreak
自由消解张量后出现的同调是 Tor。一个正则序列的 Koszul 消解在商模上常变成零微分复形，因而能直接计算这些同调；商环有多个分支或非零幂零元，并不自动破坏原序列的正则性。
\end{chaptersummary}

\BookExercises

\begin{exercise}\label{b3:ex:15-module-choice}
在 \(R=k[X,Y]\) 上，分别判断 \(X\) 在 \(R/(Y)\)、\(R/(X)\)、\(R/(XY)\) 上是否为非零因子；在成立时，判断单元素序列 \(X\) 是否正则。
\end{exercise}
\begin{solution}
第一模同构于 \(k[X]\)，乘 \(X\) 单射且商为 \(k\)，故序列正则。第二模中 \(X\) 作用为零而模非零，故不是非零因子。第三模中非零的 \(\bar Y\) 被 \(X\) 零化，也不成立。
\end{solution}

\begin{exercise}\label{b3:ex:15-powers}
设 \(R\) 为交换环，\(M\ne0\) 为 \(R\) 模，\(x,y\) 是 \(M\) 正则序列。证明对任意正整数 \(a,b\)，\(x^a,y^b\) 仍为 \(M\) 正则序列。
\end{exercise}
\begin{solution}
\(x^a\) 显然单射。证明 \(y\) 在 \(M/x^aM\) 上单射：若 \(ym=x^an\)，模掉 \(xM\) 得 \(m=xm_1\)，约去 \(x\) 后有 \(ym_1=x^{a-1}n\)，反复进行得 \(m\in x^aM\)。所以 \(y^b\) 也在该商上单射。末端商 \(M/(x^a,y^b)M\) 满射到非零的 \(M/(x,y)M\)，因而非零。
\end{solution}

\begin{exercise}\label{b3:ex:15-order-localization}
对正文的模 \(M=k[X,Y]\oplus k[X,Y]/(X-1,Y)\)，在极大理想 \((X,Y)\) 处局部化。解释为何原来不正则的次序 \(Y,X\) 在这里变成正则。
\end{exercise}
\begin{solution}
\(X-1\) 在该局部环中是单位，故第二直和项局部化为零，剩下 \(k[X,Y]_{(X,Y)}\)。先模掉 \(Y\) 得 \(k[X]_{(X)}\)，其中 \(X\) 为非零因子，末端商为 \(k\)，所以 \(Y,X\) 正则。原来的乘法核全部位于被这次局部化删去的支集部分。
\end{solution}

\begin{exercise}\label{b3:ex:15-flat-not-faithful}
取 \(R=k[T]\times k[T]\)，\(S=k[T]\) 为投影到第一因子的 \(R\) 代数，\(x=(T,0)\)。证明 \(S\) 平坦但不忠实，并说明 \(x\) 在 \(S\) 上正则不能推出它在 \(R\) 上正则。
\end{exercise}
\begin{solution}
\(S\) 作为 \(R\) 模是幂等元 \((1,0)\) 给出的直和项，故投射、从而平坦。非零模 \(0\times k[T]\) 与 \(S\) 张量为零，所以不忠实。\(x\) 在 \(S\) 上就是 \(T\)，乘法单射且商为 \(k\)；在 \(R\) 上却零化非零元 \((0,1)\)。
\end{solution}

\begin{exercise}\label{b3:ex:15-koszul-localize-unit}
把 \(k[X,Y]\) 上的 Koszul 复形 \(K_\bullet(X,Y;R)\) 在素理想 \((X)\) 处局部化。计算其所有同调，并解释局部化后的序列是否仍按本章约定正则。
\end{exercise}
\begin{solution}
\(Y\) 成为单位，故 \(h=Y^{-1}h_Y\) 满足 \(dh+hd=\operatorname{id}\)，全部同调为零。末端商也为零，因此该序列不满足本章正则序列的非退化条件。局部化保持单射，却不一定保持末端商非零。
\end{solution}

\begin{exercise}\label{b3:ex:15-three-matrices}
对任意交换环 \(R\) 中的 \(x,y,z\)，在二次外幂中采用基 \(e_1\wedge e_2,e_1\wedge e_3,e_2\wedge e_3\)，写出 \(K_\bullet(x,y,z;R)\) 的三个微分矩阵，并计算相邻矩阵乘积。
\end{exercise}
\begin{solution}
三个矩阵依次为
\[
d_1=(x\ \ y\ \ z),\qquad
d_2=\begin{pmatrix}-y&-z&0\\x&0&-z\\0&x&y\end{pmatrix},
\qquad d_3=\begin{pmatrix}z\\-y\\x\end{pmatrix}.
\]
\(d_1d_2\) 的分量为 \(-xy+yx,-xz+zx,-yz+zy\)，均为零；\(d_2d_3\) 的分量为 \(-yz+zy,xz-zx,-xy+yx\)，也均为零。这里只用交换性，特征二时两项仍相消。
\end{solution}

\begin{exercise}\label{b3:ex:15-generator-operation}
设 \(R\) 为交换环，\(x,y,a\in R\)。构造 \(K_\bullet(x,y+ax;R)\) 与 \(K_\bullet(x,y;R)\) 的同构，并说明在局部有限模情形中替换 \(y\) 为 \(y+ax\) 是否改变正则性。
\end{exercise}
\begin{solution}
把新一次基 \(f_1,f_2\) 分别送到 \(e_1,e_2+ae_1\)。其微分分别为 \(x,y+ax\)，且基变换矩阵行列式为一，故在全部外幂上给出可逆链映射。张量任意 \(M\) 后仍为同构。局部有限模中若 \(x,y\in\mathfrak m\)，两序列的末端理想相同，并由 Koszul 正合性判据得到正则性等价；也可直接看出第二个元素在 \(M/xM\) 上的作用没有改变。
\end{solution}

\begin{exercise}\label{b3:ex:15-repeated-equation}
设 \(R\) 为整环，\(0\ne x\in R\) 为非单位元。计算 \(K_\bullet(x,x;R)\) 的全部同调。
\end{exercise}
\begin{solution}
一次圈满足 \(x(a+b)=0\)，所以为 \((-c,c)\)。一次边界为 \((-xc,xc)\)，故 \(H_1\cong R/(x)\)。最高微分单射，\(H_2=0\)，而 \(H_0=R/(x)\)。重复方程提供了一个新的一次同调，而非另一个正则元素。
\end{solution}

\begin{exercise}\label{b3:ex:15-dual-numbers}
令 \(R=k[\varepsilon]/(\varepsilon^2)\)。分别计算 \(K_\bullet(\varepsilon;R)\) 与 \(K_\bullet(\varepsilon;R/(\varepsilon))\) 的同调，并比较原因。
\end{exercise}
\begin{solution}
第一复形中乘法核为 \((\varepsilon)\cong k\)，余核也为 \(k\)。第二复形的微分为零，所以两处同调都为 \(k\)。前者的非正合性来自环中真实的零因子，后者则因为张量系数模使乘法完全消失。
\end{solution}

\begin{exercise}\label{b3:ex:15-crossing-homology}
在 \(R=k[X,Y]/(XY)\) 中计算 \(H_i(\bar X,\bar Y;R)\)。
\end{exercise}
\begin{solution}
把 \(R\) 写成常数项相同的多项式配对，可知 \(\operatorname{Ann}(\bar X)=(\bar Y)\)、\(\operatorname{Ann}(\bar Y)=(\bar X)\)，两理想交为零。一次圈恰为 \((\bar Ya,\bar Xb)\)，边界为 \((-\bar Yc,\bar Xc)\)。模掉边界后，\((\bar Y,0)\) 与 \((0,\bar X)\) 互为相反数，而且均被 \(\bar X,\bar Y\) 零化；任意圈都化成该类的 \(k\) 倍。这个类非零，否则须有 \(c\) 同时满足 \(-\bar Yc=\bar Y\)、\(\bar Xc=0\)，前式要求其常数项为 \(-1\)，后式要求为零，矛盾。因此 \(H_1\cong k\)，最高同调为上述零化子之交，故 \(H_2=0\)，而 \(H_0=k\)。
\end{solution}

\begin{exercise}\label{b3:ex:15-triangular-complete-intersection}
在 \(R=k[X,Y,Z]\) 中证明 \(Y-X^2,Z-Y^2,Z\) 是正则序列，计算末端商的 \(k\) 维数，并写出自由消解各项的秩。
\end{exercise}
\begin{solution}
依次前两次商为 \(k[X,Z]\)、\(k[X]\)，所以前两步乘法都是整环中非零元的乘法。此时 \(Z\) 的像为 \(X^4\)，仍为非零因子，末端商为 \(k[X]/(X^4)\)，维数为四。三元 Koszul 消解从次数零至三的秩依次为 \(1,3,3,1\)。
\end{solution}

\begin{exercise}\label{b3:ex:15-component-section}
令 \(A=k[U,V]/(UV)\)。对 \(a,b\in k\)，判断 \(aU+bV\) 何时在 \(A\) 上为非零因子，并在成立时求其商环。
\end{exercise}
\begin{solution}
若 \(a=0\)，则该元素被非零的 \(U\) 零化；\(b=0\) 同理。若 \(a,b\ne0\)，用常数项相同的配对模型，乘法分别为乘 \(aU,bV\)，故单射。在商中 \(V=-ab^{-1}U\)，关系 \(UV=0\) 变成 \(U^2=0\)，所以商环为 \(k[U]/(U^2)\)。
\end{solution}

\begin{exercise}\label{b3:ex:15-tor-integers}
设 \(m,n\) 为正整数，计算 \(\operatorname{Tor}_i^{\mathbb Z}(\mathbb Z/m,\mathbb Z/n)\) 对所有 \(i\ge0\) 的值。
\end{exercise}
\begin{solution}
用 \(\mathbb Z\xrightarrow{m}\mathbb Z\) 的自由消解。张量后为 \(\mathbb Z/n\xrightarrow{m}\mathbb Z/n\)。若 \(d=\gcd(m,n)\)，其核由 \(n/d\) 的余类生成，阶为 \(d\)，余核也为 \(\mathbb Z/d\)。因此次数零、一均为 \(\mathbb Z/d\)，其余次数为零。
\end{solution}

\begin{exercise}\label{b3:ex:15-intersection-tor}
设 \(R\) 为交换环，\(I,J\) 为理想。证明
\[
\operatorname{Tor}_1^R(R/I,R/J)\cong(I\cap J)/IJ.
\]
用此计算 \(R=k[X,Y]\)、\(I=(X^2)\)、\(J=(XY)\) 时的结果。
\end{exercise}
\begin{solution}
对 \(0\rightarrow I\rightarrow R\rightarrow R/I\rightarrow0\) 与 \(R/J\) 使用 Tor 长正合列，因 \(R\) 自由得到
\[
0\longrightarrow\operatorname{Tor}_1^R(R/I,R/J)
\longrightarrow I/JI\longrightarrow R/J.
\]
最后映射的核是 \((I\cap J)/IJ\)，即得同构。给定例子中 \(I\cap J=(X^2Y)\)、\(IJ=(X^3Y)\)，乘 \(X^2Y\) 给出 \((I\cap J)/IJ\cong R/(X)\)。
\end{solution}

\begin{exercise}\label{b3:ex:15-residue-tor}
令 \(R=k[X_1,\ldots,X_d]_{(X_1,\ldots,X_d)}\)、\(k=R/(X_1,\ldots,X_d)\)。计算 \(\dim_k\operatorname{Tor}_i^R(k,k)\)，并说明为何答案在 \(i>d\) 时为零。
\end{exercise}
\begin{solution}
变量逐次取商仍为剩余变量的多项式局部环，故构成正则序列。其 Koszul 自由消解长为 \(d\)，张量 \(k\) 后所有微分为零。因此次数 \(i\) 的同调就是 \(\bigwedge^ik^d\)，维数为 \(\binom di\)；次数超过 \(d\) 时复形本身为零。
\end{solution}

\begin{exercise}\label{b3:ex:15-zero-generator-splitting}
设 \(R\) 为交换环，\(M\) 为 \(R\) 模，\(x=(x_1,\ldots,x_r)\)。证明
\[
H_i(x,0;M)\cong H_i(x;M)\oplus H_{i-1}(x;M),
\]
并由此计算 \(r\) 个零元素在 \(M\) 上的 Koszul 同调。
\end{exercise}
\begin{solution}
新增元素为零时，分解 \(K_i(x,0;M)=K_i(x;M)\oplus K_{i-1}(x;M)\) 的微分是 \((a,b)\mapsto(da,-db)\)，两个分量不混合，故同调分裂如式。从空序列的唯一同调 \(M\) 开始反复应用，得到 \(H_i(0,\ldots,0;M)\cong M^{\binom ri}\)，超出 \(0\le i\le r\) 的次数为零。
\end{solution}

\begin{exercise}\label{b3:ex:15-infinite-module-caution}
设 \(R\) 为离散赋值环，均匀参数为 \(\pi\)，分式域为 \(K\)。计算 \(K_\bullet(\pi;K)\) 的同调，说明为何不能由次数一同调为零断言 \(\pi\) 是本章意义的 \(K\) 正则序列。
\end{exercise}
\begin{solution}
\(\pi:K\rightarrow K\) 是同构，所以两处同调都为零。末端商 \(K/\pi K=0\)，违反非退化条件。\(K\) 不是有限 \(R\) 模，Nakayama 引理不能用于证明末端商非零，所以局部有限模判据不适用。
\end{solution}

\begin{exercise}\label{b3:ex:15-local-detection-obstruction}
对 \(R=k[X,Y]\) 中的序列 \(X^2,XY\)，求正次数 Koszul 同调的支集。判断在素理想 \((X)\)、\((Y)\) 处分别发生什么。
\end{exercise}
\begin{solution}
正文算出唯一非零正次数同调为 \(R/(X)\)，支集为 \(V(X)\)。在 \((X)\) 处它仍非零，故仍有正则性障碍；此时 \(Y\) 为单位，序列的理想等于 \((X)\)，第二个方程冗余。在 \((Y)\) 处 \(X^2\) 为单位，全部同调消失，但末端商也为零，故仍不构成本章非退化意义的正则序列。
\end{solution}
